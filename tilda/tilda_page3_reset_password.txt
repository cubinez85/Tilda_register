



Предпросмотр

Опубликовать
Еще
Сброс пароля
Код будет выполнен на опубликованной странице (нажмите «Опубликовать» в правом верхнем углу
<style>
  .tilda-registration-form {
    max-width: 500px;
    margin: 0 auto;
    font-family: Arial, sans-serif;
    padding: 20px;
    box-sizing: border-box;
  }
  .tilda-registration-form h3 {
    text-align: center;
    margin-bottom: 20px;
    color: #333;
  }
  .tilda-registration-form label {
    display: block;
    margin: 12px 0 4px;
    font-weight: bold;
    color: #333;
  }
  .tilda-registration-form input[type="password"] {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
    box-sizing: border-box;
  }
  .tilda-registration-form button {
    background: #007bff;
    color: white;
    border: none;
    padding: 12px 20px;
    font-size: 16px;
    border-radius: 4px;
    cursor: pointer;
    width: 100%;
    margin-top: 20px;
  }
  .tilda-registration-form button:hover {
    background: #0056b3;
  }
  .tilda-registration-form .error {
    color: red;
    font-size: 14px;
    margin-top: 8px;
  }
  .tilda-registration-form .success {
    color: green;
    font-weight: bold;
    text-align: center;
    margin-top: 15px;
  }
</style>

<div class="tilda-registration-form">
  <h3>Сброс пароля</h3>
  <input type="hidden" id="resetToken" value="">

  <label for="newPassword">Новый пароль *</label>
  <input type="password" id="newPassword" minlength="6" required>

  <label for="confirmPassword">Подтвердите пароль *</label>
  <input type="password" id="confirmPassword" minlength="6" required>

  <!-- Обработчик прямо на кнопке -->
  <button type="button" onclick="handleResetPassword()">Сохранить новый пароль</button>
  <div id="resetMessage"></div>
</div>

<script>
// Функция сброса пароля
async function handleResetPassword() {
    const messageDiv = document.getElementById('resetMessage');
    const tokenInput = document.getElementById('resetToken');
    const newPasswordInput = document.getElementById('newPassword');
    const confirmPasswordInput = document.getElementById('confirmPassword');

    // Получаем токен из URL (только при первом запуске)
    if (!tokenInput.value) {
        const urlParams = new URLSearchParams(window.location.search);
        const token = urlParams.get('token');
        if (token) {
            tokenInput.value = token;
        } else {
            messageDiv.innerHTML = '<div class="error">Ссылка недействительна</div>';
            return;
        }
    }

    const token = tokenInput.value;
    const newPassword = newPasswordInput.value;
    const confirmPassword = confirmPasswordInput.value;

    // Валидация
    if (newPassword !== confirmPassword) {
        messageDiv.innerHTML = '<div class="error">Пароли не совпадают</div>';
        return;
    }
    if (newPassword.length < 6) {
        messageDiv.innerHTML = '<div class="error">Пароль должен быть не менее 6 символов</div>';
        return;
    }

    try {
        messageDiv.innerHTML = '<div class="info">Отправка...</div>';
        const res = await fetch('https://test-register-tilda.cubinez.ru/api/reset-password', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                token: token,
                new_password: newPassword,
                confirm_password: confirmPassword
            })
        });

        const result = await res.json();

        if (res.ok) {
  messageDiv.innerHTML = '<div class="success">Пароль успешно изменён!</div>';
  setTimeout(() => {
    window.location.href = 'https://project15827036.tilda.ws/';
  }, 2000);
  } else {
            messageDiv.innerHTML = `<div class="error">${result.error || 'Неизвестная ошибка'}</div>`;
        }
    } catch (err) {
        messageDiv.innerHTML = `<div class="error">Ошибка сети: ${err.message}</div>`;
    }
}

// Автоматически подставляем токен при загрузке страницы
document.addEventListener('DOMContentLoaded', function () {
    const urlParams = new URLSearchParams(window.location.search);
    const token = urlParams.get('token');
    const tokenInput = document.getElementById('resetToken');
    const messageDiv = document.getElementById('resetMessage');

    if (!token) {
        messageDiv.innerHTML = '<div class="error">Ссылка недействительна</div>';
    } else {
        tokenInput.value = token;
    }
});
</script>