<style>
  .tilda-form {
    max-width: 500px;
    margin: 0 auto;
    font-family: Arial, sans-serif;
    padding: 20px;
    box-sizing: border-box;
  }
  .tilda-form h3 {
    text-align: center;
    margin-bottom: 20px;
    color: #333;
  }
  .tilda-form label {
    display: block;
    margin: 12px 0 4px;
    font-weight: bold;
    color: #333;
  }
  .tilda-form input {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
    box-sizing: border-box;
  }
  .tilda-form button {
    background: #007bff;
    color: white;
    border: none;
    padding: 12px 20px;
    font-size: 16px;
    border-radius: 4px;
    cursor: pointer;
    width: 100%;
    margin-top: 20px;
  }
  .tilda-form button:hover {
    background: #0056b3;
  }
  .tilda-form .message {
    margin-top: 15px;
    padding: 10px;
    border-radius: 4px;
    text-align: center;
  }
  .tilda-form .error {
    background: #ffecec;
    color: #d32f2f;
  }
  .tilda-form .success {
    background: #e8f5e9;
    color: #2e7d32;
  }
</style>

<div class="tilda-form">
  <h3>Восстановление пароля</h3>
  <label for="fpEmail">Электронная почта *</label>
  <input type="email" id="fpEmail" placeholder="example@mail.ru" required>

  <button type="button" onclick="requestPasswordReset()">Отправить ссылку</button>
  <div id="fpMessage"></div>
</div>

<script>
async function requestPasswordReset() {
    const email = document.getElementById('fpEmail').value.trim();
    const msgDiv = document.getElementById('fpMessage');

    if (!email) {
        msgDiv.innerHTML = '<div class="message error">Пожалуйста, введите email.</div>';
        return;
    }

    try {
        const response = await fetch('https://test-register-tilda.cubinez.ru/api/forgot-password', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email })
        });

        const result = await response.json();
        const isSuccess = response.status === 200;

        msgDiv.innerHTML = `
            <div class="message ${isSuccess ? 'success' : 'error'}">
                ${result.message || result.error}
            </div>
        `;
    } catch (error) {
        console.error('Ошибка:', error);
        msgDiv.innerHTML = '<div class="message error">Не удалось подключиться к серверу.</div>';
    }
}
</script>